# Backlog dos adapters FreeBSD

1. Somente leitura: uname, sysctl, mount, fstab, pkg info, service status, testparm -s, smbstatus, wbinfo, CUPS e quotas.
2. Validação real de capacidades: pacote Samba, opções de compilação, AD DC, Kerberos, DNS, vfs_full_audit e acl_xattr.
3. Backup atômico de configuração e metadados.
4. Aplicação de compartilhamentos com testparm e reload.
5. ACL NFSv4 com exportação, resolução SID/UID/GID e testes efetivos.
6. Ingresso controlado como membro do AD; nunca promoção automática a AD DC.
7. CUPS, drivers, Point and Print e validação de GPO.
8. Quotas UFS, syslog/SIEM e empacotamento pkg.
